---
topic: Pal3 UX — DESIGN.md + EXPERIENCE.md spines
updated: 2026-08-11T00:00
---

- (event) UX run started — intent: create, project paluwagan3
- (note) environment: uv and python unavailable; customize.toml read directly for {workflow.*} defaults, memlog maintained by hand in script-compatible format (same precedent as the brief/PRD/architecture runs)
- (note) no prior UX runs in _bmad-output/planning-artifacts/ux-designs — greenfield
- (note) persistent_facts glob for project-context.md matched nothing — no brownfield code context exists yet
- (note) candidate sources surfaced (paths only, unread in parent): briefs/brief-paluwagan3-2026-08-08/{brief.md,addendum.md}, prds/prd-paluwagan3-2026-08-08/prd.md, architecture/architecture-paluwagan3-2026-08-08/{ARCHITECTURE-SPINE.md,SCF-architecture-outline.md}
- (decision by user) sources: PRD only (prd.md), inherited by reference — brief and architecture not bound
- (decision by user) working mode: Fast path — batch gaps, draft both spines with [ASSUMPTION] tags, user corrects in review
- (note) hard constraint carried from the PRD run: SCF #45 submission due 2026-08-16, five days out
- (note) stakes read as consumer + regulated — real money, Philippine cohort, DPA 2012 in scope, and members whose prior experience of paluwagan may be losing money
- (decision by user) underwriter UX lives in the SAME PWA, role-gated — V1 has exactly one underwriter (the founder), so a second surface cannot pay for itself in an eight-week window
- (decision by user) crypto framing: plain language, honest mechanics — never volunteer "stablecoin"/"escrow"/"blockchain", never imply money is somewhere it isn't, vocabulary available on tap
- (decision by user) English only — no i18n layer in V1
- (decision by user) notifications: in-app only for V1
- (concern raised by Sally, accepted by user) in-app-only notification is the product's weakest link: the 48h grace window ends in an automated slash + 150-point penalty, and the only warning lives on a screen the member has no external prompt to open; spec'd as chosen, with SMS escalation written into EXPERIENCE.md as a named deferred decision to revisit before the mainnet pilot
- (decision by user) visual temperament: calm and institutional — should look like something still running unchanged when round 8 settles, not like a fintech pitch
- (decision by user) light mode only
- (note) user gave no brain dump and no brand assets across two probes; imports/ is empty and the entire visual direction is derived from temperament + cohort, tagged [ASSUMPTION] at the top of DESIGN.md § Brand & Style
- (decision) money-state color is reserved absolutely: paid/grace/default only; accent teal means commitment and never status — a member should learn what color means in this product in one round
- (decision) every monetary figure uses tabular-nums; the schedule is a column of money and proportional figures make it unscannable
- (decision) dollar is the number of record everywhere; indicative peso is meta-styled, always "≈", never inside a button label or on the FX acknowledgement — tagged [ASSUMPTION] because showing peso at all trades comprehension against implying a stability the product disclaims
- (decision) no shadows anywhere — hierarchy from tone, position, and type weight; a product claiming nothing is hidden should not be built on things floating above other things
- (decision) named token {spacing.commit-gap} = 32px with a rule attached: minimum distance between the last disclosure and any control that commits funds
- (decision) NO confirmation dialogs on commit screens — a second "are you sure?" modal trains dismissal and adds nothing over a screen whose whole purpose is the decision; the disclosure carries the weight instead
- (decision) primary funding actions are labeled with act + amount ("Lock $46.80 and join"), never "Confirm" or "Continue"
- (decision) no avatars, names, or photos on member rows — Pal3 does not hold legal identity and the UI must not imply it does
- (decision) payout is stated, never celebrated — no confetti, no animation; it arrived because it was always going to
- (insight) SM-5 (>=80% of cohort can state their score and why it changed) is a MEASURED UX outcome, not a product one — drove an invented EXPERIENCE.md section: score/consequence/history always together, thresholds stated in stake terms not score terms, never a badge or level
- (decision) invented EXPERIENCE.md sections beyond the standard spine: Money Legibility, Irreversibility & Commitment, Trust Score Legibility, Notification & Reach
- (gap) IA closure incomplete: Account and Activity surfaces have no journey landing on them — they exist because a WalletConnect session and an irreversible on-chain history need somewhere to live; flagged rather than back-filled with an invented journey
- (gap) FR-16 removal-after-stake-exhaustion has a specified state but no journey of its own; currently the far edge of UJ-3
- (note) Inspiration & Anti-patterns section omitted — user supplied no references and inventing them would be authoring rather than capturing
- (note) PRD-fixed foundation, not open for UX to redecide: mobile-first PWA (§12), WalletConnect connection (FR-21), 5-10 member single Room, weekly cadence, USD-pegged stablecoin with recorded FX acknowledgement (FR-22)
- (event) both spines drafted: DESIGN.md with full token frontmatter (16 colors, 7 typography roles, rounded, spacing, 11 components) + all 8 body sections; EXPERIENCE.md with 20 surfaces mapped to FRs, 8 standard sections, 4 invented sections, 4 key flows mirroring UJ-1..UJ-4
- (decision by user) indicative peso RESOLVED: shown, meta-styled, "≈" prefixed, never in a button label or on the FX acknowledgement, always with rate source and timestamp on commitment surfaces — comprehension for a peso-thinking cohort outweighs the implied-stability risk, conditional on the attribution rule holding
- (decision by user) reviewer gate SKIPPED — five days to the SCF deadline and UX is not a submission artifact; Validate mode remains available if the build surfaces problems
- (decision by user) key-screen mocks scoped to Room terms + Commit only; all other IA surfaces build from spine tables alone
- (event) mock rendered at .working/key-room-terms-commit.html, two states (terms unacknowledged / commit funded+acknowledged), promoted to mockups/room-terms-commit.html and inline-linked from EXPERIENCE.md IA + Irreversibility and DESIGN.md Components
- (note) input reconciliation SKIPPED — imports/ is empty, no user-supplied visuals exist to reconcile against
- (note) doc_standards polish passes (bmad-editorial-review-structure, bmad-editorial-review-prose) were NOT run; flagged to the user rather than silently skipped
- (assumption) open, non-blocking, carried into the spines: whole aesthetic direction derived rather than supplied (DESIGN.md Brand & Style); no UI system named so both spines specify from first principles rather than as a delta (EXPERIENCE.md Foundation); "add to calendar" on schedule rows proposed as the cheapest external reach available without notification infrastructure
- (event) spines finalized — status set to final on both
- (event) UX run resumed 2026-08-12 — intent: update; three items handed back from the architecture update (ARCHITECTURE-SPINE.md, now 17 ADs)
- (note) architecture is now bound as a source by reference — AD-16 and AD-17 constrain UX decisions the spines had not made
- (insight) the handback framed React + Vite as the "UI system" EXPERIENCE.md Foundation was waiting for; corrected before deciding — React supplies a rendering framework, not components, so the component tables cannot collapse to deltas on its account, and adopting a component system is a separate question that was put to the user rather than assumed
- (decision by user) member handle is a short alphanumeric code — fixed length, unambiguous alphabet excluding 0/O and 1/I, separated into two groups, set in tabular figures; reads as an account reference, which suits the institutional temperament and keeps two members from being confusable
- (constraint) handle rejected word pairs as pushing toward playful against DESIGN.md's no-mascots posture and reading as a nickname the product assigned; rejected truncated addresses because a shared prefix is genuinely easy to misread in a UI whose highest-harm failure is misreading who owes what
- (decision by user) FX rate freshness window is one hour — the peso line stays present almost always, so members are never trained to expect it missing; the API omits an out-of-window rate under AD-16 and the client then renders no peso line at all
- (constraint) freshness rejected 15 minutes because a comprehension aid that intermittently vanishes on a flaky connection is worse than one slightly stale but always present; rejected 24 hours because a day-old rate stretches what "indicative" can carry on a screen where someone commits eight weeks of savings
- (decision by user) NO component system adopted — both spines continue to specify from first principles; the sparse visual language (no shadows, one accent, outline-only pills) would mostly override a library's defaults, and a styled system would be fought on nearly every component
- (constraint) consequence of specifying from first principles, accepted knowingly: focus traversal, ARIA wiring, dialog semantics and the aria-live grace banner are hand-built rather than inherited from tested primitives — the Accessibility Floor is now a build risk carried by the solo developer, and is recorded as such rather than left implicit
- (note) React + Vite + vite-plugin-pwa recorded in Foundation as the implementation substrate, NOT as an inherited UI system; the Foundation assumption that no UI system is named is now resolved rather than stale
- (event) spines updated — DESIGN.md gains a handle typography role; EXPERIENCE.md Foundation, Money Legibility, Component Patterns and Accessibility Floor amended
- (insight) checking the promoted mock against the attribution rule exposed an ambiguity in the rule's own wording: "wherever a peso figure appears" reads per-figure, but the mock attributes once per screen and leaves the inline terms-row peso unattributed
- (decision) attribution granularity resolved: stated once per surface beneath the primary amount, governing every peso figure on that surface; a surface with no attribution anywhere shows no peso figures at all
- (gap) mockups/room-terms-commit.html states a timestamp ("rate as of 11 Aug, 9:41") but names no source, which AD-16 requires; not corrected in this run because the FX provider is not yet selected and inventing one would be authoring — a two-string fix once the provider is chosen
- (decision by user) reviewer gate RUN for this update; lens menu presented, user picked the rubric walker only (accessibility, money-legibility and regulated-language lenses offered and declined)
- (note) rubric walker ran INLINE rather than as a subagent — subagents unavailable in this session, disclosed to the user as a weaker check than the gate specifies; written to review-rubric.md
- (question) CRITICAL found by rubric walker: state-grace #A5701A measures 3.86:1 on its own wash, 4.26:1 on raised, 3.93:1 on base — failing WCAG AA for body text everywhere it appears, while EXPERIENCE.md holds the grace banner to AAA; the two spines contradicted each other on the highest-stakes element in the product
- (change) state-grace darkened to #6B4710 — 7.51:1 on the wash, 8.29:1 on raised, AAA on both, still unmistakably amber rather than red
- (change) DESIGN.md § Colors gains a measured-contrast table for all load-bearing pairs; the root cause of the defect was that qualitative commitments (AA, AAA) had no numbers to check them against
- (question) HIGH found by rubric walker: no empty/cold state for Trust score with no history — the state EVERY member is in at pilot start, on the surface SM-5 measures — nor for Activity or Applicants
- (change) three State Patterns rows added for the cold states; the trust-score one names the first event that will move the score rather than showing an encouragement
- (question) MEDIUM found by rubric walker: button-secondary had a visual spec but no behavioral row, and the pair named the same component "Button (primary)" in DESIGN.md and "Primary action" in EXPERIENCE.md — a downstream consumer extracting component names would get two lists
- (change) Component Patterns row added for Button (secondary); primary row renamed to match DESIGN.md
- (note) MEDIUM left open: FR-16 removal still has no flow of its own, already self-flagged in the IA closure note; MEDIUM left open: the mock's rate attribution names no source, pending provider selection
- (event) spines finalized after reviewer gate — both status final, updated 2026-08-12
